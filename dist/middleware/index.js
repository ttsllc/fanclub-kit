/**
 * v0.1 — placeholder. v0.2 will export `requireTier(routeMap)` for
 * Next.js App Router middleware. Until then, tenants can hand-roll a
 * Supabase session check + `memberCanAccess()` from `core/tier-gate`.
 */
export const __PHASE_2_PLACEHOLDER = true;
//# sourceMappingURL=index.js.map