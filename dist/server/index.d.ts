/**
 * v0.1 — placeholder. Real exports land in v0.2:
 *   - createSupabaseServerClient(cookies)
 *   - signupAction, loginAction, signoutAction
 *   - createCheckoutSession(member, tier)
 *   - handleStripeWebhook(req)
 *
 * For now consumers stub these locally and we'll merge upstream once
 * the contracts settle on the first real instance (explosion-candy-fanclub).
 */
export declare const __PHASE_2_PLACEHOLDER = true;
//# sourceMappingURL=index.d.ts.map